% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/postie.R
\name{get_epi_outputs}
\alias{get_epi_outputs}
\title{Post-process an ODE run into postie-format rates and prevalence.}
\usage{
get_epi_outputs(
  x,
  diagnostic = "lm",
  rates_args = list(),
  prevalence_args = list(),
  ...
)
}
\arguments{
\item{x}{output of \code{\link[=run_simulation_ode]{run_simulation_ode()}}.}

\item{diagnostic}{prevalence diagnostic, "lm" or "pcr".}

\item{rates_args}{named list of extra arguments for \code{\link[postie:get_rates]{postie::get_rates()}}
(e.g. \code{scaler}, \code{treatment_scaler}, \code{life_expectancy}, \code{infer_ft}).}

\item{prevalence_args}{named list of extra arguments for
\code{\link[postie:get_prevalence]{postie::get_prevalence()}}.}

\item{...}{shared arguments accepted by both postie functions (e.g.
\code{baseline_year}, \code{ages_as_years}).}
}
\value{
list(rates, prevalence).
}
\description{
Returns a list with a \code{rates} table (long: one row per timestep x age band,
with \code{clinical}, \code{severe}, \code{mortality}, \code{yld}, \code{yll}, \code{dalys}, \code{person_days})
and a \code{prevalence} table (wide: one \verb{<diagnostic>_prevalence_<lo>_<hi>} column
per age band), matching \code{postie::get_rates()} / \code{postie::get_prevalence()}.
}
\examples{
\dontrun{
p <- malariasimulation::get_parameters()
out <- run_simulation_ode(timesteps = 3650, parameters = p, init_EIR = 20)
epi <- get_epi_outputs(out, diagnostic = "lm")
head(epi$rates)
epi$prevalence$lm_prevalence_2_10
}
}

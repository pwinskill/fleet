% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{run_simulation_ode}
\alias{run_simulation_ode}
\title{Run the mean-field (ODE) malaria model.}
\usage{
run_simulation_ode(
  timesteps,
  parameters,
  init_EIR = NULL,
  age_lower = default_age_lower(),
  n_eir = 10L,
  n_foim = 10L,
  n_eip = 20L,
  atol = 1e-08,
  rtol = 1e-08,
  step_size_max = 1,
  odin_file = NULL
)
}
\arguments{
\item{timesteps}{number of days to simulate. Output has \code{timesteps + 1} rows
(day 0 is the seeded equilibrium; days 1..timesteps are integrated).}

\item{parameters}{a malariasimulation::get_parameters() list (falciparum),
optionally with interventions layered on via the \verb{set_*} builders. The
following are applied automatically from the list (no extra arguments):
clinical treatment (time-varying), antimalarial resistance, bed nets, IRS,
MDA/SMC/PMC, PEV (EPI + mass) and TBV vaccines, seasonality and flexible
carrying capacity, and custom demography (\code{set_demography}: age-specific
mortality and the resulting equilibrium age structure). P. vivax is rejected;
the model is always compartmental (the individual-mosquito path does not
apply). See the README for the mean-field approximations used by each module.}

\item{init_EIR}{target adult EIR (bites/adult/year). If NULL, taken from
\code{parameters$init_EIR} (set by malariasimulation::set_equilibrium()).}

\item{age_lower}{age-group lower edges in \strong{years} (default graded grid).}

\item{n_eir, n_foim, n_eip}{Erlang-chain stage counts for the EIR lag, FOIM lag
and mosquito EIP. Larger values sharpen the (otherwise gamma-shaped) lags
toward the IBM's fixed delays; equilibrium is exact for any value.}

\item{atol, rtol, step_size_max}{dust2 ODE-solver controls. The defaults
(\code{1e-8}, \code{1e-8}, \code{1}) preserve the flat equilibrium exactly; for long dynamic
projections a looser tolerance and larger step cap (e.g. \code{1e-6}, \code{1e-6}, \code{10})
run several times faster with negligible effect on aggregate outputs.}

\item{odin_file}{optional path to the odin source (development use).}
}
\value{
a wide, malariasimulation-style daily count table, one row per output
day. Columns:
\itemize{
\item \code{timestep} — output day (0..timesteps), the row key.
\item per age band (tags in \strong{days}, \verb{<lo>_<hi>}): \verb{n_age_*} (population),
\verb{n_detect_lm_*} and \verb{p_detect_lm_*} (LM-positive count and PfPR
proportion), \verb{n_detect_pcr_*}, \verb{n_inc_clinical_*}, \verb{n_inc_severe_*} and
\verb{n_inc_*} (all-infection incidence) — all per-day counts.
\item \code{ft} (treated fraction), \code{EIR} (per adult per year), \code{FOIM}.
\item population-total infection-state counts \code{S_count}, \code{D_count},
\code{A_count}, \code{U_count}, \code{Tr_count}, \code{Ph_count} (diagnostic).
}
Incidence columns are per-day counts; do not thin rows before postie. Can be
passed directly to postie::get_rates() / postie::get_prevalence().
}
\description{
Run the mean-field (ODE) malaria model.
}
\details{
Seasonal runs oscillate around a limit cycle rather than holding flat;
the state is seeded at the annual-mean (aseasonal) equilibrium, so the first
~10 years are a transient onto the cycle and the seasonal annual-mean EIR
sits a few percent below the aseasonal \code{init_EIR} target (nonlinear
averaging). Use a burned-in cycle for calibration/comparison.
}
\examples{
\dontrun{
p <- malariasimulation::get_parameters()

# daily wide count table, init_EIR supplied directly
out <- run_simulation_ode(timesteps = 3650, parameters = p, init_EIR = 20)
head(out[, c("timestep", "n_age_730_3650", "n_detect_lm_730_3650", "EIR")])

# or let set_equilibrium() seed init_EIR into the parameter list
p2 <- malariasimulation::set_equilibrium(
  malariasimulation::get_parameters(), init_EIR = 5)
out2 <- run_simulation_ode(timesteps = 3650, parameters = p2)
}
}

% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/run.R
\name{run_simulation_ode}
\alias{run_simulation_ode}
\title{Run the mean-field (ODE) malaria model.}
\usage{
run_simulation_ode(
  timesteps,
  parameters = NULL,
  correlations = NULL,
  init_EIR = NULL,
  age_lower = default_age_lower(),
  n_eir = 10L,
  n_foim = 10L,
  n_eip = 20L,
  n_ph = NULL,
  n_phc = NULL,
  atol = 1e-08,
  rtol = 1e-08,
  step_size_max = 1,
  odin_file = NULL
)
}
\arguments{
\item{timesteps}{number of days to simulate. Output has \code{timesteps + 1} rows
(day 0 is the seeded equilibrium; days 1..timesteps are integrated).}

\item{parameters}{a malariasimulation::get_parameters() list (falciparum),
optionally with interventions layered on via the \verb{set_*} builders. The
following are applied automatically from the list (no extra arguments):
clinical treatment (time-varying), antimalarial resistance, bed nets, IRS,
MDA/SMC/PMC, PEV (EPI + mass) and TBV vaccines, seasonality and flexible
carrying capacity, and custom demography (\code{set_demography}: age-specific
mortality and the resulting equilibrium age structure). P. vivax is rejected;
the model is always compartmental (the individual-mosquito path does not
apply). See the README for the mean-field approximations used by each module.

Two optional fields on the list tune how faithfully the mean field mirrors the
IBM's per-individual arithmetic. Both default to the validated choice, so you
normally leave them alone:
\itemize{
\item \code{bite_dedup} (default \code{1}) — reproduce the IBM's per-timestep bite
deduplication. malariasimulation collects the day's bitten individuals in a
bitset, so a person bitten repeatedly in one timestep is infected at most
once: the daily infection probability is \code{(1 - exp(-EPS)) * b}, which
saturates. Setting \code{0} uses the unbounded \code{b * EPS} instead, which
over-predicts infection where exposure approaches one bite/person/day
(seasonal peaks, high-\code{zeta} strata) but makes the \code{malariaEquilibrium}
seed an exact fixed point — useful for equilibrium tests.
\item \code{acquired_immunity_offset} (default \code{0}) — the IBM adds \code{+0.5} to
positive acquired immunity inside the \code{b}/\code{phi}/\code{theta} Hill functions. That
is a per-individual detail which does not carry over to a stratum mean; an
A/B against the IBM ensemble mean favours \code{0}. Set \code{0.5} to reproduce the
IBM's literal Hill calls.
\item \code{hold_init_EIR} (default \code{FALSE}) — only matters with \code{set_demography()}.
malariasimulation's \code{set_equilibrium()} sizes the mosquito population from
the equilibrium under its \emph{default} exponential age structure, so under a
custom demography the IBM drifts to whatever transmission that density
supports. By default blink replicates that: it takes the IBM's mosquito
density and seeds at the EIR its own equilibrium under the custom age
structure then supports (a fixed point, so no burn-in), which is generally
\emph{not} \code{init_EIR}. Set \code{TRUE} to seed at \code{init_EIR} exactly instead.
}}

\item{correlations}{accepted so the first three arguments mirror
\code{malariasimulation::run_simulation(timesteps, parameters, correlations)}
exactly (drop-in call compatibility). Intervention correlation is an
individual-level feature with no mean-field analogue, so a non-NULL value is
ignored with a warning (the ODE assumes independence between interventions).}

\item{init_EIR}{target adult EIR (bites/adult/year), with the same meaning as in
\code{malariasimulation::set_equilibrium()}. If NULL, taken from
\code{parameters$init_EIR} (set by set_equilibrium()). Under the default demography
this is the EIR blink realises; under \code{set_demography()} see \code{hold_init_EIR}
above.}

\item{age_lower}{age-group lower edges in \strong{years} (default graded grid).}

\item{n_eir, n_foim, n_eip}{Erlang-chain stage counts for the EIR lag, FOIM lag
and mosquito EIP. Larger values sharpen the (otherwise gamma-shaped) lags
toward the IBM's fixed delays; equilibrium is exact for any value.}

\item{n_ph, n_phc}{Erlang-chain stage counts for the post-treatment (\code{Ph}) and
chemoprevention (\code{Ph_c}) prophylaxis compartments. \code{NULL} (default) matches the
chain's variance to the drug's Weibull protection curve, capped at 20. For
\code{Ph_c} that is \verb{1/CV²} of the Weibull: 14 for SP-AQ, 15 for DHA-PQP. \code{Ph}
follows the exponential treated stage \code{Tr}, so its count matches the variance
of the whole \code{Tr + Ph} sojourn and its mean is the integrated protection left
after \code{Tr}: 16 stages for SP-AQ, 20 for DHA-PQP, and 1 for AL, whose 10-day
protection is already less variable than \code{Tr} itself. A drug mixture is
moment-matched as a mixture. \code{1} is a single exponential stage, which for
\code{Ph_c} leaks protection early between monthly SMC rounds. The count is fixed
at the seed's drug mix — a first-line switch moves the chain's mean, not its
shape.}

\item{atol, rtol, step_size_max}{dust2 ODE-solver controls. The defaults
(\code{1e-8}, \code{1e-8}, \code{1}) preserve the flat equilibrium exactly; for long dynamic
projections a looser relative tolerance and larger step cap (\code{atol = 1e-8},
\code{rtol = 1e-6}, \code{step_size_max = 10}) run several times faster with negligible
effect on aggregate outputs. Keep \code{atol} at \code{1e-8}: the individual prophylaxis
chain stages hold occupancies of order \code{1e-6}, which a looser absolute
tolerance lets dip below zero.}

\item{odin_file}{optional path to the odin source (development use).}
}
\value{
a wide, malariasimulation-style daily count table, one row per output
day. Columns:
\itemize{
\item \code{timestep} — output day (0..timesteps), the row key.
\item per age band (tags in \strong{days}, \verb{<lo>_<hi>}): \verb{n_age_*} (population),
\verb{n_detect_lm_*} and \verb{p_detect_lm_*} (LM-positive count and PfPR
proportion), \verb{n_detect_pcr_*}, \verb{n_inc_clinical_*}, \verb{n_inc_severe_*} and
\verb{n_inc_*} (all-infection incidence) — all per-day counts.
\item \code{ft} (treated fraction), \code{EIR} (per adult per year), \code{FOIM}.
\item population-total infection-state counts \code{S_count}, \code{D_count},
\code{A_count}, \code{U_count}, \code{Tr_count}, \code{Ph_count} (diagnostic).
}
Incidence columns are per-day counts; do not thin rows before postie. Can be
passed directly to postie::get_rates() / postie::get_prevalence().
}
\description{
Run the mean-field (ODE) malaria model.
}
\details{
Seasonal runs oscillate around a limit cycle rather than holding flat;
the state is seeded at the annual-mean (aseasonal) equilibrium, so the first
~10 years are a transient onto the cycle and the seasonal annual-mean EIR
sits a few percent below the aseasonal \code{init_EIR} target (nonlinear
averaging). Use a burned-in cycle for calibration/comparison.
}
\examples{
\dontrun{
p <- malariasimulation::get_parameters()

# daily wide count table, init_EIR supplied directly
out <- run_simulation_ode(timesteps = 3650, parameters = p, init_EIR = 20)
head(out[, c("timestep", "n_age_730_3650", "n_detect_lm_730_3650", "EIR")])

# or let set_equilibrium() seed init_EIR into the parameter list
p2 <- malariasimulation::set_equilibrium(
  malariasimulation::get_parameters(), init_EIR = 5)
out2 <- run_simulation_ode(timesteps = 3650, parameters = p2)
}
}
